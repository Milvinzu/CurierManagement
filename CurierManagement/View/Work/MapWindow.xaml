<Window x:Class="CurierManagement.View.Work.MapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CurierManagement.View.Work"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="📍 Оберіть локацію на карті - Харків"
        Height="700" 
        Width="1000"
        WindowStartupLocation="CenterOwner"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Padding="20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🗺️" 
                          FontSize="20" 
                          Margin="0,0,10,0" 
                          VerticalAlignment="Center"/>
                <TextBlock Text="Виберіть точку доставки та скопіюйте посилання" 
                          FontSize="16" 
                          FontWeight="SemiBold" 
                          Foreground="White" 
                          VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <wv2:WebView2 Grid.Row="1" 
                     x:Name="MapWebView" 
                     Source="https://www.google.com/maps/place/Kharkiv,+Kharkiv+Oblast,+Ukraine/@49.9935,36.2304,13z"/>

        <!-- Footer with URL input -->
        <Border Grid.Row="2" 
                BorderThickness="0,1,0,0"
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                          Text="📋 Інструкція: 1) Знайдіть потрібну точку на карті 2) Скопіюйте URL з адресного рядка браузера 3) Вставте його нижче 4) Натисніть 'Зберегти'"
                          FontSize="12" 
                          TextWrapping="Wrap"
                          Margin="0,0,0,15"/>

                <!-- URL Input and Buttons -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" 
                              Text="🔗 URL карти:" 
                              FontWeight="Medium" 
                              VerticalAlignment="Center"
                              Margin="0,0,10,0"/>

                    <TextBox Grid.Column="1" 
                            x:Name="MapUrlTextBox"
                            Text="{Binding MapUrl, UpdateSourceTrigger=PropertyChanged}" 
                            FontSize="12"
                            Padding="10,8"
                            Margin="0,0,10,0"
                            VerticalAlignment="Center"/>

                    <Button Grid.Column="2" 
                           Content="✅ Зберегти" 
                           Command="{Binding SaveMapUrlCommand}"
                           IsEnabled="{Binding HasValidUrl}"
                           Margin="0,0,10,0"/>

                    <Button Grid.Column="3" 
                           Content="❌ Скасувати" 
                           Command="{Binding CancelCommand}"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#2196F3"/>
        <SolidColorBrush x:Key="PrimaryDarkColor" Color="#1976D2"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TextColor" Color="#212121"/>
        <SolidColorBrush x:Key="SecondaryTextColor" Color="#757575"/>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{StaticResource BorderColor}"/>
                                <Setter Property="Foreground" Value="{StaticResource SecondaryTextColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SurfaceColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        </Style>
    </Window.Resources>
</Window>